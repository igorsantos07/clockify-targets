// import { redirect } from '@sveltejs/kit'

//FIXME this is breaking the build; it could probably be resolved upgrading to v2, so we don't need to throw redirects anymore
// throw redirect(301, '/targets')
